# capturas/

Deixa aqui os **Win+Shift+S** do jogo (painel do personagem, arvore,
equipamento, charms, bestiario). Ficam fora do git.

A leitura por visao (o Claude local le a imagem e escreve na `vault.db` com
`source = 'captura'`) chega na **ordem 4**. Ate la as imagens ficam a espera —
nao se perdem, nao se apagam.

Nada aqui abre o jogo: sao so imagens que tu tiraste.
