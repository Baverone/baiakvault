# capturas de ecra do Andre: sao dele, nao vao para o git (so o README fica)
capturas/*
!capturas/README.md

__pycache__/
*.pyc
*.log
*.tmp

# rascunhos locais de exploracao
scripts/_rascunho*.py

# token de escrita do modo de edicao (nasce no primeiro `serve`)
data/serve.token
